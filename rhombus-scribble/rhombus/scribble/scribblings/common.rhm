#lang rhombus/static/and_meta
import:
  meta_label:
    rhombus open:
      except:
        #{#%module-begin}
    rhombus/scribble/manual open

export:
  meta_label:
    all_from(rhombus)
    all_from(rhombus/scribble/manual)
